services:
  - type: web
    name: stock-crypto-app
    env: docker
    buildCommand: docker build -t stock-crypto-app ./
    startCommand: docker run -p 8501:8501 stock-crypto-app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
